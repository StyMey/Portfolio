{% extends 'base.html.twig' %}

{% block title %}Projets{% endblock %}

{% block body %}
    <h1>Mes projets</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ project.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ project.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ project.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('projects_index') }}">Retour à la liste</a>

    <a href="{{ path('projects_edit', {'id': project.id}) }}">Modifier</a>

    {{ include('admin/projects/_delete_form.html.twig') }}
{% endblock %}
